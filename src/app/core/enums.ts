export enum paths {
  auth = 'auth',
  foods = 'alimentos',
  foodCategories = 'categorias_de_alimentos',
  home = 'inicio',
  login = 'login',
  notFound = 'not_found', 
  register = 'register',
}

export enum resources {
  foods = 'Foods',
  foodCategories = 'FoodCategories'
}